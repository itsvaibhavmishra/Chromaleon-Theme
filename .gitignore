node_modules/
*.vsix
.DS_Store

# Build output — all generated by `npm run build`. Anchored with a leading slash:
# an unanchored `icons/` also matches `src/icons/`, which is source.
/themes/
/icons/
/dist/
/src/generated.ts
